## Packages
recharts | Dashboard analytics charts and data visualization
framer-motion | Page transitions and scroll-triggered animations
date-fns | Human-readable date formatting

## Notes
Authentication is handled via Replit Auth (use-auth.ts).
File uploads (PDF) use multipart/form-data.
Settings include sensitive SMTP passwords which should be handled carefully (though usually backend handles encryption, frontend sends plain text over HTTPS).
